global:
  scrape_interval: 2s
  evaluation_interval: 2s

rule_files:
  - "/etc/prometheus/alerts.yml"

scrape_configs:
  - job_name: 'sleipnir'
    static_configs:
      - targets: ['sleipnir-gateway:8080']
